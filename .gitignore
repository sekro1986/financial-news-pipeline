.venv/
__pycache__/
*.pyc
*.db
.env
*.log

# Liste de flux personnelle (créée depuis feeds.txt.example)
feeds.txt

# Liste de flux disclosures (créée depuis disclosure_feeds.txt.example)
disclosure_feeds.txt

# Watchlist live (créée depuis watchlist.yaml.example)
watchlist.yaml

# Etat local runtime (healthcheck, agent) + sauvegardes
heartbeat.txt
health_state.json
agent_state.json
backups/
calibration.json
